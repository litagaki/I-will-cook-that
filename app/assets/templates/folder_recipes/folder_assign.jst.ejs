<button class="cancel">×</button>

<div class="group">
  <% folders.each(function(folder) { %>
    <label>
      <input
        type="checkbox"
        name="folder_recipe[folder_id]"
        value="<%= folder.id %>"
        <%= savedFolders.get(folder.id) ? "checked" : "" %>>
        <%= folder.escape("title") %>
  </label>
  <%  }) %>
</div>



<button class="submit folder-recipe-submit">Update Saved Folder(s)</button>
